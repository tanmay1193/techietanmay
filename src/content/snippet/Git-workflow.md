---
title: "Git Workflow"
slug: "gitworkflow"
description: "Common commands for feature branching and merging"
publishDate: "14 Aug 2025"
tags: ["Git","Workflow"]
---

```bash
# Check status
git status

# Stage changes
git add .
git status

# Commit changes
git commit -m "Add feature"
git status

# Dry-run before pushing
git push --dry-run

# Push branch
git push origin main
```